Hello <%= @order.name %>,

Here is the reciept for your order.

Items
-----

<% @order.order_items.each do |item| %>
  <%= item.quantity %> <%= item.item.name %>
<% end %>

Total: <%= @order.total_amount %>